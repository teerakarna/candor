name: Publish docs

on:
  push:
    branches: [main]
    paths:
      - 'docs-site/**'
      - '.github/workflows/publish-docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  publish:
    # SHA-pinned to teerakarna/mkdocs-publisher's main, same discipline as every third-party
    # action in this repo (verified with `git -C <clone of mkdocs-publisher> rev-parse main`,
    # not guessed) - bump manually when that repo's workflow changes.
    uses: teerakarna/mkdocs-publisher/.github/workflows/publish.yml@c2ac253310e1e7751babbe3b133d8716a2747ab1 # main
    with:
      working-directory: docs-site
